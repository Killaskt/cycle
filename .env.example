# Local Supabase (see .claude/skills/local-supabase-stack/SKILL.md).
# These exact values are the FIXED local demo keys `supabase start` always
# prints — safe to commit as an example, never valid against the hosted
# project. Copy this file to .env.local (gitignored) to use them for real.
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0

# Optional client telemetry. Add the real DSN as a Codemagic secret named
# VITE_SENTRY_DSN; it is safe to embed in the client but is not needed locally.
VITE_SENTRY_DSN=
