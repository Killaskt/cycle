function App() {
  // Screens (Intake, System, Today, I Fell Off, cycle-close review) are
  // built ticket-by-ticket — see .scratch/system-generator/issues/.
  return <div>Cycle</div>
}

export default App
